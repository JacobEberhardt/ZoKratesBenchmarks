import "hashes/sha256/512bit" as sha256

def main(field[256] a, field[256] b) -> (field[256]):
	field[256] digest = sha256(a, a)
	for field i in 0..19 do
		field[256] temp = sha256(digest, digest)
		digest = temp
	endfor
	return digest